from pathlib import Path

# The folder that contains the "config" directory and the "endpoint" package
repo_root = Path(__file__).parent.parent
# Entry-point endpoint
endpoint_name = "compute-portal-entry-point"
